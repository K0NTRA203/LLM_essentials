from chatgpt_wrapper import ChatGPT
import os
import jsonlines
import json
import subprocess
import sqlite3
import numpy as np
import time
from flask import Flask, request, jsonify

bot = ChatGPT()
response = bot.ask('salam, answer shortest')
print(response,bot.conversation_id,bot.parent_message_id)